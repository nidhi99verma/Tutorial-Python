#lambda or anonymous function(rudece line of code)
def add(a,b):
    return a+b
# or
add2 = lambda a,b:a+b  # lambda parameter:condition
print(add2(2,3))
multiply = lambda a,b:a*b
print(multiply(2,3))
print(add)           #in this function have name add
print(add2)          #in this lambda function use so no name of function only lambda
print(multiply)      #in this lambda function use so no name of function only lambda